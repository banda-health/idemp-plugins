# Generated Schema for M_MovementLineMA - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 11 - $Id$
extend type Query {
	M_MovementLineMAGet(page: Int, size: Int, sort: String, filter: String): M_MovementLineMAConnection!
}

extend type Mutation {
	M_MovementLineMASave(entity: M_MovementLineMAInput!): M_MovementLineMA!
	M_MovementLineMASaveMany(entities: [M_MovementLineMAInput!]!): [M_MovementLineMA!]!
	M_MovementLineMADelete(uuids: [String!]!): Boolean!
}

type M_MovementLineMAConnection {
	results: [M_MovementLineMA!]!
	pagingInfo: PagingInfo!
}

type M_MovementLineMA {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# Time used for LIFO and FIFO Material Policy
	DateMaterialPolicy: Date
	# The record is active in the system
	IsActive: Boolean!
	IsAutoGenerated: Boolean!
	# Product Attribute Set Instance
	M_AttributeSetInstance: M_AttributeSetInstance!
	# Inventory Move document Line
	M_MovementLine: M_MovementLine!
	UUID: ID!
	# Quantity of a product moved.
	MovementQty: BigDecimal
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
}

input M_MovementLineMAInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Time used for LIFO and FIFO Material Policy
	DateMaterialPolicy: Date
	# The record is active in the system
	IsActive: Boolean
	IsAutoGenerated: Boolean
	# Product Attribute Set Instance
	M_AttributeSetInstance: ForeignEntityInput
	# Inventory Move document Line
	M_MovementLine: ForeignEntityInput
	UUID: ID
	# Quantity of a product moved.
	MovementQty: BigDecimal
}
