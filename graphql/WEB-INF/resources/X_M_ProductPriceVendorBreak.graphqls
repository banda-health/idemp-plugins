# Generated Schema for M_ProductPriceVendorBreak - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	M_ProductPriceVendorBreakGet(page: Int, size: Int, sort: String, filter: String): M_ProductPriceVendorBreakConnection!
}

extend type Mutation {
	M_ProductPriceVendorBreakSave(entity: M_ProductPriceVendorBreakInput!): M_ProductPriceVendorBreak!
	M_ProductPriceVendorBreakDelete(uuids: [String!]!): Boolean!
}

type M_ProductPriceVendorBreakConnection {
	results: [M_ProductPriceVendorBreak!]!
	pagingInfo: PagingInfo!
}

type M_ProductPriceVendorBreak {
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# Low Value of trade discount break level
	BreakValue: Int!
	# Identifies a Business Partner
	C_BPartner: C_BPartner
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# The record is active in the system
	IsActive: Boolean!
	# Identifies a unique instance of a Price List
	M_PriceList_Version: M_PriceList_Version!
	# Product, Service, Item
	M_Product: M_Product!
	ID: ID!
	# Lowest price for a product
	PriceLimit: Int!
	# List Price
	PriceList: Int!
	# Standard Price
	PriceStd: Int!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
}

input M_ProductPriceVendorBreakInput {
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# Low Value of trade discount break level
	BreakValue: Int
	# Identifies a Business Partner
	C_BPartner: ForeignEntityInput
	# The record is active in the system
	IsActive: Boolean
	# Identifies a unique instance of a Price List
	M_PriceList_Version: ForeignEntityInput
	# Product, Service, Item
	M_Product: ForeignEntityInput
	ID: ID
	# Lowest price for a product
	PriceLimit: Int
	# List Price
	PriceList: Int
	# Standard Price
	PriceStd: Int
}
