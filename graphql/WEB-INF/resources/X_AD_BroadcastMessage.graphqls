# Generated Schema for AD_BroadcastMessage - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	AD_BroadcastMessageGet(page: Int, size: Int, sort: String, filter: String): AD_BroadcastMessageConnection!
}

extend type Mutation {
	AD_BroadcastMessageSave(entity: AD_BroadcastMessageInput!): AD_BroadcastMessage!
	AD_BroadcastMessageSaveMany(entities: [AD_BroadcastMessageInput!]!): [AD_BroadcastMessage!]!
	AD_BroadcastMessageDelete(uuids: [String!]!): Boolean!
}

type AD_BroadcastMessageConnection {
	results: [AD_BroadcastMessage!]!
	pagingInfo: PagingInfo!
}

type AD_BroadcastMessage {
	UUID: ID!
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# Responsibility Role
	AD_Role: AD_Role
	# User within the system - Internal or Business Partner Contact
	AD_User: AD_User
	# How Many Times Message Should be Broadcasted
	BroadcastFrequency: AD_Ref_List!
	# Broadcast Message
	BroadcastMessage: String!
	# Type of Broadcast
	BroadcastType: AD_Ref_List!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# Expire On
	Expiration: Date
	Expired: Boolean!
	# Expire the broadcast message
	ExpireNow: String
	# The record is active in the system
	IsActive: Boolean!
	# The Topic is published and can be viewed
	IsPublished: Boolean!
	# Want to Log the Acknowledgement of Message?
	LogAcknowledge: Boolean!
	# The document has been processed
	Processed: Boolean!
	# Publish Broadcast Message
	Publish: String
	# Target client
	Target: AD_Ref_List!
	# Test broadcast message
	TestMessage: String
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
}

input AD_BroadcastMessageInput {
	UUID: ID
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# Responsibility Role
	AD_Role: ForeignEntityInput
	# User within the system - Internal or Business Partner Contact
	AD_User: ForeignEntityInput
	# How Many Times Message Should be Broadcasted
	BroadcastFrequency: AD_Ref_ListInput
	# Broadcast Message
	BroadcastMessage: String
	# Type of Broadcast
	BroadcastType: AD_Ref_ListInput
	# Expire On
	Expiration: Date
	Expired: Boolean
	# Expire the broadcast message
	ExpireNow: String
	# The record is active in the system
	IsActive: Boolean
	# The Topic is published and can be viewed
	IsPublished: Boolean
	# Want to Log the Acknowledgement of Message?
	LogAcknowledge: Boolean
	# The document has been processed
	Processed: Boolean
	# Publish Broadcast Message
	Publish: String
	# Target client
	Target: AD_Ref_ListInput
	# Test broadcast message
	TestMessage: String
}
