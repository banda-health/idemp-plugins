# Generated Schema for M_StorageReservation - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 11 - $Id$
extend type Query {
	M_StorageReservation(UU: String!): M_StorageReservation
	M_StorageReservationGet(Page: Int, Size: Int, Sort: String, Filter: String): M_StorageReservationConnection!
}

extend type Mutation {
	M_StorageReservationSave(Entity: M_StorageReservationInput!): M_StorageReservation!
	M_StorageReservationSaveMany(Entities: [M_StorageReservationInput!]!): [M_StorageReservation!]!
	M_StorageReservationDelete(UUs: [String!]!): Boolean!
}

type M_StorageReservationConnection {
	Results: [M_StorageReservation!]!
	PagingInfo: PagingInfo!
}

type M_StorageReservation {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Date of Last Inventory Count
	DateLastInventory: Date
	# The record is active in the system
	IsActive: Boolean!
	# This is a Sales Transaction
	IsSOTrx: Boolean!
	# Product Attribute Set Instance
	M_AttributeSetInstance: M_AttributeSetInstance!
	# Product, Service, Item
	M_Product: M_Product!
	UU: ID!
	# Storage Warehouse and Service Point
	M_Warehouse: M_Warehouse!
	# Quantity
	Qty: BigDecimal!
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
}

input M_StorageReservationInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Date of Last Inventory Count
	DateLastInventory: Date
	# The record is active in the system
	IsActive: Boolean
	# This is a Sales Transaction
	IsSOTrx: Boolean
	# Product Attribute Set Instance
	M_AttributeSetInstance: ForeignEntityInput
	# Product, Service, Item
	M_Product: ForeignEntityInput
	UU: ID
	# Storage Warehouse and Service Point
	M_Warehouse: ForeignEntityInput
	# Quantity
	Qty: BigDecimal
}
