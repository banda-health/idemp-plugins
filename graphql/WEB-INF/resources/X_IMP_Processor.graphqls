# Generated Schema for IMP_Processor - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 12 - $Id$
extend type Query {
	IMP_Processor(UU: String!): IMP_Processor
	IMP_ProcessorGet(Page: Int, Size: Int, Sort: String, Filter: String): IMP_ProcessorConnection!
}

extend type Mutation {
	IMP_ProcessorSave(Entity: IMP_ProcessorInput!): IMP_Processor!
	IMP_ProcessorSaveMany(Entities: [IMP_ProcessorInput!]!): [IMP_Processor!]!
	IMP_ProcessorDelete(UUs: [String!]!): Boolean!
}

type IMP_ProcessorConnection {
	Results: [IMP_Processor!]!
	PagingInfo: PagingInfo!
}

type IMP_Processor {
	Account: String
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Date the process was last run.
	DateLastRun: DateTime
	# Date the process will run next
	DateNextRun: DateTime
	# Optional short description of the record
	Description: String
	# Frequency of events
	Frequency: Int!
	# Frequency of event
	FrequencyType: AD_Ref_List!
	# Comment or Hint
	Help: String
	Host: String
	IMP_Processor_Type: IMP_Processor_Type!
	UU: ID!
	# The record is active in the system
	IsActive: Boolean!
	# Number of days to keep the log entries
	KeepLogDays: Int!
	# Alphanumeric identifier of the entity
	Name: String!
	PasswordInfo: String
	Port: Int
	Processing: Boolean!
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
	# Search key for the record in the format required - must be unique
	Value: String!
}

input IMP_ProcessorInput {
	Account: String
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Date the process was last run.
	DateLastRun: DateTime
	# Date the process will run next
	DateNextRun: DateTime
	# Optional short description of the record
	Description: String
	# Frequency of events
	Frequency: Int
	# Frequency of event
	FrequencyType: ForeignEntityInput
	# Comment or Hint
	Help: String
	Host: String
	IMP_Processor_Type: ForeignEntityInput
	UU: ID
	# The record is active in the system
	IsActive: Boolean
	# Number of days to keep the log entries
	KeepLogDays: Int
	# Alphanumeric identifier of the entity
	Name: String
	PasswordInfo: String
	Port: Int
	Processing: Boolean
	# Search key for the record in the format required - must be unique
	Value: String
}
