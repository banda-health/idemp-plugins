# Generated Schema for QM_Specification - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 11 - $Id$
extend type Query {
	QM_SpecificationGet(page: Int, size: Int, sort: String, filter: String): QM_SpecificationConnection!
}

extend type Mutation {
	QM_SpecificationSave(entity: QM_SpecificationInput!): QM_Specification!
	QM_SpecificationSaveMany(entities: [QM_SpecificationInput!]!): [QM_Specification!]!
	QM_SpecificationDelete(uuids: [String!]!): Boolean!
}

type QM_SpecificationConnection {
	results: [QM_Specification!]!
	pagingInfo: PagingInfo!
}

type QM_Specification {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Workflow or combination of tasks
	AD_Workflow: AD_Workflow
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean!
	# Product Attribute Set
	M_AttributeSet: M_AttributeSet!
	# Product, Service, Item
	M_Product: M_Product!
	# Alphanumeric identifier of the entity
	Name: String
	# BOM & Formula
	PP_Product_BOM: PP_Product_BOM
	UUID: ID!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
	# Valid from including this date (first day)
	ValidFrom: Date
	# Valid to including this date (last day)
	ValidTo: Date
	# Search key for the record in the format required - must be unique
	Value: String
}

input QM_SpecificationInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Workflow or combination of tasks
	AD_Workflow: ForeignEntityInput
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean
	# Product Attribute Set
	M_AttributeSet: ForeignEntityInput
	# Product, Service, Item
	M_Product: ForeignEntityInput
	# Alphanumeric identifier of the entity
	Name: String
	# BOM & Formula
	PP_Product_BOM: ForeignEntityInput
	UUID: ID
	# Valid from including this date (first day)
	ValidFrom: Date
	# Valid to including this date (last day)
	ValidTo: Date
	# Search key for the record in the format required - must be unique
	Value: String
}
