# Generated Schema for PA_MeasureCalc - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 12 - $Id$
extend type Query {
	PA_MeasureCalc(UU: String!): PA_MeasureCalc
	PA_MeasureCalcGet(Page: Int, Size: Int, Sort: String, Filter: String): PA_MeasureCalcConnection!
}

extend type Mutation {
	PA_MeasureCalcSave(Entity: PA_MeasureCalcInput!): PA_MeasureCalc!
	PA_MeasureCalcSaveMany(Entities: [PA_MeasureCalcInput!]!): [PA_MeasureCalc!]!
	PA_MeasureCalcDelete(UUs: [String!]!): Boolean!
}

type PA_MeasureCalcConnection {
	Results: [PA_MeasureCalc!]!
	PagingInfo: PagingInfo!
}

type PA_MeasureCalc {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Database Table information
	AD_Table: AD_Table!
	# Fully qualified Business Partner key column (C_BPartner_ID)
	BPartnerColumn: String
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Fully qualified date column
	DateColumn: String!
	# Optional short description of the record
	Description: String
	# Dictionary Entity Type; Determines ownership and synchronization
	AD_EntityType: AD_EntityType!
	# The record is active in the system
	IsActive: Boolean!
	# Key Column for Table
	KeyColumn: String!
	# Alphanumeric identifier of the entity
	Name: String!
	# Fully qualified Organization column (AD_Org_ID)
	OrgColumn: String!
	UU: ID!
	# Fully qualified Product column (M_Product_ID)
	ProductColumn: String
	# SQL SELECT clause
	SelectClause: String!
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
	# Fully qualified SQL WHERE clause
	WhereClause: String!
}

input PA_MeasureCalcInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Database Table information
	AD_Table: ForeignEntityInput
	# Fully qualified Business Partner key column (C_BPartner_ID)
	BPartnerColumn: String
	# Fully qualified date column
	DateColumn: String
	# Optional short description of the record
	Description: String
	# Dictionary Entity Type; Determines ownership and synchronization
	AD_EntityType: ForeignEntityInput
	# The record is active in the system
	IsActive: Boolean
	# Key Column for Table
	KeyColumn: String
	# Alphanumeric identifier of the entity
	Name: String
	# Fully qualified Organization column (AD_Org_ID)
	OrgColumn: String
	UU: ID
	# Fully qualified Product column (M_Product_ID)
	ProductColumn: String
	# SQL SELECT clause
	SelectClause: String
	# Fully qualified SQL WHERE clause
	WhereClause: String
}
