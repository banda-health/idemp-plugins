# Generated Schema for AD_ChartDatasource - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 11 - $Id$
extend type Query {
	AD_ChartDatasourceGet(Page: Int, Size: Int, Sort: String, Filter: String): AD_ChartDatasourceConnection!
}

extend type Mutation {
	AD_ChartDatasourceSave(Entity: AD_ChartDatasourceInput!): AD_ChartDatasource!
	AD_ChartDatasourceSaveMany(Entities: [AD_ChartDatasourceInput!]!): [AD_ChartDatasource!]!
	AD_ChartDatasourceDelete(UUs: [String!]!): Boolean!
}

type AD_ChartDatasourceConnection {
	Results: [AD_ChartDatasource!]!
	PagingInfo: PagingInfo!
}

type AD_ChartDatasource {
	AD_Chart: AD_Chart!
	UU: ID!
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Database Table information
	AD_Table: AD_Table
	# Fully qualified data category column
	CategoryColumn: String
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# Fully qualified date column
	DateColumn: String
	# Optional short description of the record
	Description: String
	# Dictionary Entity Type; Determines ownership and synchronization
	AD_EntityType: AD_EntityType!
	# SQL FROM clause
	FromClause: String!
	# The record is active in the system
	IsActive: Boolean!
	# Key Column for Table
	KeyColumn: String
	# Alphanumeric identifier of the entity
	Name: String!
	SeriesColumn: String
	# Number of time units to offset displayed chart data from the current date.
	TimeOffset: Int
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
	# Fully qualified data value column
	ValueColumn: String!
	# Fully qualified SQL WHERE clause
	WhereClause: String
}

input AD_ChartDatasourceInput {
	AD_Chart: ForeignEntityInput
	UU: ID
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Database Table information
	AD_Table: ForeignEntityInput
	# Fully qualified data category column
	CategoryColumn: String
	# Fully qualified date column
	DateColumn: String
	# Optional short description of the record
	Description: String
	# Dictionary Entity Type; Determines ownership and synchronization
	AD_EntityType: ForeignEntityInput
	# SQL FROM clause
	FromClause: String
	# The record is active in the system
	IsActive: Boolean
	# Key Column for Table
	KeyColumn: String
	# Alphanumeric identifier of the entity
	Name: String
	SeriesColumn: String
	# Number of time units to offset displayed chart data from the current date.
	TimeOffset: Int
	# Fully qualified data value column
	ValueColumn: String
	# Fully qualified SQL WHERE clause
	WhereClause: String
}
