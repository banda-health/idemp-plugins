# Generated Schema for AD_TreeBar - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 7.1 - $Id$
extend type Query {
	AD_TreeBarGet(page: Int, size: Int, sort: String, filter: String): AD_TreeBarConnection!
}

extend type Mutation {
	AD_TreeBarSave(entity: AD_TreeBarInput!): AD_TreeBar!
	AD_TreeBarDelete(uuids: [String!]!): Boolean!
}

type AD_TreeBarConnection {
	results: [AD_TreeBar!]!
	pagingInfo: PagingInfo!
}

type AD_TreeBar {
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Identifies a Menu
	AD_Menu: AD_Menu
	# Organizational entity within client
	AD_Org: AD_Org!
	# Identifies a Tree
	AD_Tree: AD_Tree!
	ID: ID!
	# User within the system - Internal or Business Partner Contact
	AD_User: AD_User!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# The record is active in the system
	IsActive: Boolean!
	# This record is a favourite
	IsFavourite: Boolean!
	# Determine the order items will be automatically opened when user logs in
	LoginOpenSeqNo: Int
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
}

input AD_TreeBarInput {
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# Identifies a Tree
	AD_Tree: ForeignEntityInput
	ID: ID
	# User within the system - Internal or Business Partner Contact
	AD_User: ForeignEntityInput
	# The record is active in the system
	IsActive: Boolean
	# This record is a favourite
	IsFavourite: Boolean
	# Determine the order items will be automatically opened when user logs in
	LoginOpenSeqNo: Int
}
