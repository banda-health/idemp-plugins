# Generated Schema for A_Asset_Use - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 12 - $Id$
extend type Query {
	A_Asset_Use(UU: String!): A_Asset_Use
	A_Asset_UseGet(Page: Int, Size: Int, Sort: String, Filter: String, Where: String, Parameters: [Object!]): A_Asset_UseConnection!
}

extend type Mutation {
	A_Asset_UseSave(Entity: A_Asset_UseInput!): A_Asset_Use!
	A_Asset_UseSaveMany(Entities: [A_Asset_UseInput!]!): [A_Asset_Use!]!
	A_Asset_UseDelete(UUs: [String!]!): Boolean!
}

type A_Asset_UseConnection {
	Results: [A_Asset_Use!]!
	PagingInfo: PagingInfo!
}

type A_Asset_Use {
	# Asset used internally or by customers
	A_Asset: A_Asset!
	UU: ID!
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean!
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
	UseDate: Date!
	# Currently used units of the assets
	UseUnits: Int!
}

input A_Asset_UseInput {
	# Asset used internally or by customers
	A_Asset: ForeignEntityInput
	UU: ID
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean
	UseDate: Date
	# Currently used units of the assets
	UseUnits: Int
}
