# Generated Schema for A_Asset_Use - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	A_Asset_UseGet(page: Int, size: Int, sort: String, filter: String): A_Asset_UseConnection!
}

extend type Mutation {
	A_Asset_UseSave(entity: A_Asset_UseInput!): A_Asset_Use!
	A_Asset_UseDelete(uuids: [String!]!): Boolean!
}

type A_Asset_UseConnection {
	results: [A_Asset_Use!]!
	pagingInfo: PagingInfo!
}

type A_Asset_Use {
	# Asset used internally or by customers
	A_Asset: A_Asset!
	ID: ID!
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
	UseDate: Date!
	# Currently used units of the assets
	UseUnits: Int!
}

input A_Asset_UseInput {
	# Asset used internally or by customers
	A_Asset: ForeignEntityInput
	ID: ID
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean
	UseDate: Date
	# Currently used units of the assets
	UseUnits: Int
}
