# Generated Schema for C_OrderPaySchedule - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 12 - $Id$
extend type Query {
	C_OrderPaySchedule(UU: String!): C_OrderPaySchedule
	C_OrderPayScheduleGet(Page: Int, Size: Int, Sort: String, Filter: String, Where: String, Parameters: [Object!]): C_OrderPayScheduleConnection!
}

extend type Mutation {
	C_OrderPayScheduleSave(Entity: C_OrderPayScheduleInput!): C_OrderPaySchedule!
	C_OrderPayScheduleSaveMany(Entities: [C_OrderPayScheduleInput!]!): [C_OrderPaySchedule!]!
	C_OrderPayScheduleDelete(UUs: [String!]!): Boolean!
}

type C_OrderPayScheduleConnection {
	Results: [C_OrderPaySchedule!]!
	PagingInfo: PagingInfo!
}

type C_OrderPaySchedule {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Order
	C_Order: C_Order!
	UU: ID!
	# Payment Schedule Template
	C_PaySchedule: C_PaySchedule
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Calculated amount of discount
	DiscountAmt: BigDecimal!
	# Last Date for payments with discount
	DiscountDate: Date!
	# Amount of the payment due
	DueAmt: BigDecimal!
	# Date when the payment is due
	DueDate: Date!
	# The record is active in the system
	IsActive: Boolean!
	# Element is valid
	IsValid: Boolean!
	# The document has been processed
	Processed: Boolean!
	Processing: Boolean!
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
}

input C_OrderPayScheduleInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Order
	C_Order: ForeignEntityInput
	UU: ID
	# Payment Schedule Template
	C_PaySchedule: ForeignEntityInput
	# Calculated amount of discount
	DiscountAmt: BigDecimal
	# Last Date for payments with discount
	DiscountDate: Date
	# Amount of the payment due
	DueAmt: BigDecimal
	# Date when the payment is due
	DueDate: Date
	# The record is active in the system
	IsActive: Boolean
	# Element is valid
	IsValid: Boolean
	# The document has been processed
	Processed: Boolean
	Processing: Boolean
}
