# Generated Schema for M_MatchPO - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	M_MatchPOGet(page: Int, size: Int, sort: String, filter: String): M_MatchPOConnection!
}

extend type Mutation {
	M_MatchPOSave(entity: M_MatchPOInput!): M_MatchPO!
	M_MatchPODelete(uuids: [String!]!): Boolean!
}

type M_MatchPOConnection {
	results: [M_MatchPO!]!
	pagingInfo: PagingInfo!
}

type M_MatchPO {
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# Invoice Detail Line
	C_InvoiceLine: C_InvoiceLine
	# Sales Order Line
	C_OrderLine: C_OrderLine!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# Accounting Date
	DateAcct: Date!
	# Transaction Date
	DateTrx: Date!
	# Optional short description of the record
	Description: String
	# Document sequence number of the document
	DocumentNo: String
	# The record is active in the system
	IsActive: Boolean!
	# Indicates if this document requires approval
	IsApproved: Boolean!
	# Product Attribute Set Instance
	M_AttributeSetInstance: M_AttributeSetInstance
	# Line on Shipment or Receipt document
	M_InOutLine: M_InOutLine!
	ID: ID!
	# Product, Service, Item
	M_Product: M_Product!
	# Posting status
	Posted: Boolean!
	# Difference between Purchase and Invoice Price per matched line
	PriceMatchDifference: Int
	# The document has been processed
	Processed: Boolean!
	# The date+time (expressed in decimal format) when the document has been processed
	ProcessedOn: Int
	Processing: Boolean!
	# Quantity
	Qty: Int!
	Ref_MatchPO: M_MatchPO
	# ID of document reversal
	Reversal: M_MatchPO
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
}

input M_MatchPOInput {
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# Invoice Detail Line
	C_InvoiceLine: ForeignEntityInput
	# Sales Order Line
	C_OrderLine: ForeignEntityInput
	# Accounting Date
	DateAcct: Date
	# Transaction Date
	DateTrx: Date
	# Optional short description of the record
	Description: String
	# Document sequence number of the document
	DocumentNo: String
	# The record is active in the system
	IsActive: Boolean
	# Indicates if this document requires approval
	IsApproved: Boolean
	# Product Attribute Set Instance
	M_AttributeSetInstance: ForeignEntityInput
	# Line on Shipment or Receipt document
	M_InOutLine: ForeignEntityInput
	ID: ID
	# Product, Service, Item
	M_Product: ForeignEntityInput
	# Posting status
	Posted: Boolean
	# Difference between Purchase and Invoice Price per matched line
	PriceMatchDifference: Int
	# The document has been processed
	Processed: Boolean
	# The date+time (expressed in decimal format) when the document has been processed
	ProcessedOn: Int
	Processing: Boolean
	# Quantity
	Qty: Int
	Ref_MatchPO: ForeignEntityInput
	# ID of document reversal
	Reversal: ForeignEntityInput
}
