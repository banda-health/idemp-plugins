# Generated Schema for AD_Ref_Table - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 12 - $Id$
extend type Query {
	AD_Ref_Table(UU: String!): AD_Ref_Table
	AD_Ref_TableGet(Page: Int, Size: Int, Sort: String, Filter: String, Where: String, Parameters: [Object!]): AD_Ref_TableConnection!
}

extend type Mutation {
	AD_Ref_TableSave(Entity: AD_Ref_TableInput!): AD_Ref_Table!
	AD_Ref_TableSaveMany(Entities: [AD_Ref_TableInput!]!): [AD_Ref_Table!]!
	AD_Ref_TableDelete(UUs: [String!]!): Boolean!
}

type AD_Ref_TableConnection {
	Results: [AD_Ref_Table!]!
	PagingInfo: PagingInfo!
}

type AD_Ref_Table {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Column that will display
	AD_Disp: AD_Column!
	# Info and search/select Window
	AD_InfoWindow: AD_InfoWindow
	# Unique identifier of a record
	AD_: AD_Column!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	UU: ID!
	# System Reference and Validation
	AD_Reference: AD_Reference!
	# Database Table information
	AD_Table: AD_Table!
	# Data entry or display window
	AD_Window: AD_Window
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Dictionary Entity Type; Determines ownership and synchronization
	AD_EntityType: AD_EntityType!
	# The record is active in the system
	IsActive: Boolean!
	# Displays Value column with the Display column
	IsValueDisplayed: Boolean!
	# Fully qualified ORDER BY clause
	OrderByClause: String
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
	# Fully qualified SQL WHERE clause
	WhereClause: String
}

input AD_Ref_TableInput {
	# Column that will display
	AD_Disp: ForeignEntityInput
	# Info and search/select Window
	AD_InfoWindow: ForeignEntityInput
	# Unique identifier of a record
	AD_: ForeignEntityInput
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	UU: ID
	# System Reference and Validation
	AD_Reference: ForeignEntityInput
	# Database Table information
	AD_Table: ForeignEntityInput
	# Data entry or display window
	AD_Window: ForeignEntityInput
	# Dictionary Entity Type; Determines ownership and synchronization
	AD_EntityType: ForeignEntityInput
	# The record is active in the system
	IsActive: Boolean
	# Displays Value column with the Display column
	IsValueDisplayed: Boolean
	# Fully qualified ORDER BY clause
	OrderByClause: String
	# Fully qualified SQL WHERE clause
	WhereClause: String
}
