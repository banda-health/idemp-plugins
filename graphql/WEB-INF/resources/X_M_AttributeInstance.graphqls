# Generated Schema for M_AttributeInstance - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 11 - $Id$
extend type Query {
	M_AttributeInstance(UU: String!): M_AttributeInstance
	M_AttributeInstanceGet(Page: Int, Size: Int, Sort: String, Filter: String): M_AttributeInstanceConnection!
}

extend type Mutation {
	M_AttributeInstanceSave(Entity: M_AttributeInstanceInput!): M_AttributeInstance!
	M_AttributeInstanceSaveMany(Entities: [M_AttributeInstanceInput!]!): [M_AttributeInstance!]!
	M_AttributeInstanceDelete(UUs: [String!]!): Boolean!
}

type M_AttributeInstanceConnection {
	Results: [M_AttributeInstance!]!
	PagingInfo: PagingInfo!
}

type M_AttributeInstance {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# The record is active in the system
	IsActive: Boolean!
	# Product Attribute
	M_Attribute: M_Attribute!
	UU: ID!
	# Product Attribute Set Instance
	M_AttributeSetInstance: M_AttributeSetInstance!
	# Product Attribute Value
	M_AttributeValue: M_AttributeValue
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
	# Search key for the record in the format required - must be unique
	Value: String
	ValueDate: Date
	# Numeric Value
	ValueNumber: BigDecimal
}

input M_AttributeInstanceInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# The record is active in the system
	IsActive: Boolean
	# Product Attribute
	M_Attribute: ForeignEntityInput
	UU: ID
	# Product Attribute Set Instance
	M_AttributeSetInstance: ForeignEntityInput
	# Product Attribute Value
	M_AttributeValue: ForeignEntityInput
	# Search key for the record in the format required - must be unique
	Value: String
	ValueDate: Date
	# Numeric Value
	ValueNumber: BigDecimal
}
