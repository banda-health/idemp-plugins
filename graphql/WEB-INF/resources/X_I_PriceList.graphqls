# Generated Schema for I_PriceList - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	I_PriceListGet(page: Int, size: Int, sort: String, filter: String): I_PriceListConnection!
}

extend type Mutation {
	I_PriceListSave(entity: I_PriceListInput!): I_PriceList!
	I_PriceListDelete(uuids: [String!]!): Boolean!
}

type I_PriceListConnection {
	results: [I_PriceList!]!
	pagingInfo: PagingInfo!
}

type I_PriceList {
	# Client/Tenant for this installation.
	AD_Client: AD_Client
	# Organizational entity within client
	AD_Org: AD_Org
	# The Key of the Business Partner
	BPartner_Value: String
	# Low Value of trade discount break level
	BreakValue: Int
	# Identifies a Business Partner
	C_BPartner: C_BPartner
	# The Currency for this record
	C_Currency: C_Currency
	# Unit of Measure
	C_UOM: C_UOM
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User
	# Optional short description of the record
	Description: String
	# Do not allow prices below the limit price
	EnforcePriceLimit: Boolean!
	# Messages generated from import process
	I_ErrorMsg: String
	# Has this import been processed
	I_IsImported: Boolean!
	ID: ID!
	# The record is active in the system
	IsActive: Boolean!
	# Three letter ISO 4217 Code of the Currency
	ISO_Code: String
	# This is a Sales Price List
	IsSOPriceList: Boolean!
	# Tax is included in the price 
	IsTaxIncluded: Boolean!
	# Unique identifier of a Price List
	M_PriceList: M_PriceList
	# Identifies a unique instance of a Price List
	M_PriceList_Version: M_PriceList_Version
	# Product, Service, Item
	M_Product: M_Product
	# Alphanumeric identifier of the entity
	Name: String
	# Lowest price for a product
	PriceLimit: Int
	# List Price
	PriceList: Int
	# Precision (number of decimals) for the Price
	PricePrecision: Int
	# Standard Price
	PriceStd: Int
	# The document has been processed
	Processed: Boolean!
	Processing: Boolean!
	# Key of the Product
	ProductValue: String
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User
	# Valid from including this date (first day)
	ValidFrom: Date
	# UOM EDI X12 Code
	X12DE355: String
}

input I_PriceListInput {
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# The Key of the Business Partner
	BPartner_Value: String
	# Low Value of trade discount break level
	BreakValue: Int
	# Identifies a Business Partner
	C_BPartner: ForeignEntityInput
	# The Currency for this record
	C_Currency: ForeignEntityInput
	# Unit of Measure
	C_UOM: ForeignEntityInput
	# Optional short description of the record
	Description: String
	# Do not allow prices below the limit price
	EnforcePriceLimit: Boolean
	# Messages generated from import process
	I_ErrorMsg: String
	# Has this import been processed
	I_IsImported: Boolean
	ID: ID
	# The record is active in the system
	IsActive: Boolean
	# Three letter ISO 4217 Code of the Currency
	ISO_Code: String
	# This is a Sales Price List
	IsSOPriceList: Boolean
	# Tax is included in the price 
	IsTaxIncluded: Boolean
	# Unique identifier of a Price List
	M_PriceList: ForeignEntityInput
	# Identifies a unique instance of a Price List
	M_PriceList_Version: ForeignEntityInput
	# Product, Service, Item
	M_Product: ForeignEntityInput
	# Alphanumeric identifier of the entity
	Name: String
	# Lowest price for a product
	PriceLimit: Int
	# List Price
	PriceList: Int
	# Precision (number of decimals) for the Price
	PricePrecision: Int
	# Standard Price
	PriceStd: Int
	# The document has been processed
	Processed: Boolean
	Processing: Boolean
	# Key of the Product
	ProductValue: String
	# Valid from including this date (first day)
	ValidFrom: Date
	# UOM EDI X12 Code
	X12DE355: String
}
