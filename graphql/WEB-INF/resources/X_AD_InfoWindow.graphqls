# Generated Schema for AD_InfoWindow - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 11 - $Id$
extend type Query {
	AD_InfoWindow(UU: String!): AD_InfoWindow
	AD_InfoWindowGet(Page: Int, Size: Int, Sort: String, Filter: String): AD_InfoWindowConnection!
}

extend type Mutation {
	AD_InfoWindowSave(Entity: AD_InfoWindowInput!): AD_InfoWindow!
	AD_InfoWindowSaveMany(Entities: [AD_InfoWindowInput!]!): [AD_InfoWindow!]!
	AD_InfoWindowDelete(UUs: [String!]!): Boolean!
}

type AD_InfoWindowConnection {
	Results: [AD_InfoWindow!]!
	PagingInfo: PagingInfo!
}

type AD_InfoWindow {
	# Tenant for this installation.
	AD_Client: AD_Client!
	AD_CtxHelp: AD_CtxHelp
	UU: ID!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Database Table information
	AD_Table: AD_Table!
	# Data entry or display window
	AD_Window: AD_Window
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	# Dictionary Entity Type; Determines ownership and synchronization
	AD_EntityType: AD_EntityType!
	# SQL FROM clause
	FromClause: String!
	# Comment or Hint
	Help: String
	# URL of  image
	ImageURL: String
	# The record is active in the system
	IsActive: Boolean!
	# Default value
	IsDefault: Boolean!
	# Select Distinct
	IsDistinct: Boolean!
	# When load data for info window, also load number of paging
	isLoadPageNum: Boolean!
	# Show the dashlet in the dashboard
	IsShowInDashboard: Boolean!
	# Element is valid
	IsValid: Boolean!
	# If defined, you cannot query more records as defined - the query criteria needs to be changed to query less records
	MaxQueryRecords: Int!
	# Alphanumeric identifier of the entity
	Name: String!
	# Fully qualified ORDER BY clause
	OrderByClause: String
	# Other SQL Clause
	OtherClause: String
	PagingSize: Int!
	# Purchase Order Window
	PO_Window: AD_Window
	Processing: Boolean!
	# Method of ordering records; lowest number comes first
	SeqNo: Int
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
	# Fully qualified SQL WHERE clause
	WhereClause: String
}

input AD_InfoWindowInput {
	AD_CtxHelp: ForeignEntityInput
	UU: ID
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Database Table information
	AD_Table: ForeignEntityInput
	# Data entry or display window
	AD_Window: ForeignEntityInput
	# Optional short description of the record
	Description: String
	# Dictionary Entity Type; Determines ownership and synchronization
	AD_EntityType: ForeignEntityInput
	# SQL FROM clause
	FromClause: String
	# Comment or Hint
	Help: String
	# URL of  image
	ImageURL: String
	# The record is active in the system
	IsActive: Boolean
	# Default value
	IsDefault: Boolean
	# Select Distinct
	IsDistinct: Boolean
	# When load data for info window, also load number of paging
	isLoadPageNum: Boolean
	# Show the dashlet in the dashboard
	IsShowInDashboard: Boolean
	# Element is valid
	IsValid: Boolean
	# If defined, you cannot query more records as defined - the query criteria needs to be changed to query less records
	MaxQueryRecords: Int
	# Alphanumeric identifier of the entity
	Name: String
	# Fully qualified ORDER BY clause
	OrderByClause: String
	# Other SQL Clause
	OtherClause: String
	PagingSize: Int
	# Purchase Order Window
	PO_Window: ForeignEntityInput
	Processing: Boolean
	# Method of ordering records; lowest number comes first
	SeqNo: Int
	# Fully qualified SQL WHERE clause
	WhereClause: String
}
