# Generated Schema for AD_StatusLineUsedIn - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 12 - $Id$
extend type Query {
	AD_StatusLineUsedIn(UU: String!): AD_StatusLineUsedIn
	AD_StatusLineUsedInGet(Page: Int, Size: Int, Sort: String, Filter: String): AD_StatusLineUsedInConnection!
}

extend type Mutation {
	AD_StatusLineUsedInSave(Entity: AD_StatusLineUsedInInput!): AD_StatusLineUsedIn!
	AD_StatusLineUsedInSaveMany(Entities: [AD_StatusLineUsedInInput!]!): [AD_StatusLineUsedIn!]!
	AD_StatusLineUsedInDelete(UUs: [String!]!): Boolean!
}

type AD_StatusLineUsedInConnection {
	Results: [AD_StatusLineUsedIn!]!
	PagingInfo: PagingInfo!
}

type AD_StatusLineUsedIn {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Info and search/select Window
	AD_InfoWindow: AD_InfoWindow
	# Organizational entity within tenant
	AD_Org: AD_Org!
	AD_StatusLine: AD_StatusLine!
	UU: ID!
	# Tab within a Window
	AD_Tab: AD_Tab
	# Database Table information
	AD_Table: AD_Table
	# Data entry or display window
	AD_Window: AD_Window
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Dictionary Entity Type; Determines ownership and synchronization
	AD_EntityType: AD_EntityType!
	# The record is active in the system
	IsActive: Boolean!
	# Defines if this record refers to a status line or to a help widget
	IsStatusLine: Boolean!
	# Method of ordering records; lowest number comes first
	SeqNo: Int
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
}

input AD_StatusLineUsedInInput {
	# Info and search/select Window
	AD_InfoWindow: ForeignEntityInput
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	AD_StatusLine: ForeignEntityInput
	UU: ID
	# Tab within a Window
	AD_Tab: ForeignEntityInput
	# Database Table information
	AD_Table: ForeignEntityInput
	# Data entry or display window
	AD_Window: ForeignEntityInput
	# Dictionary Entity Type; Determines ownership and synchronization
	AD_EntityType: ForeignEntityInput
	# The record is active in the system
	IsActive: Boolean
	# Defines if this record refers to a status line or to a help widget
	IsStatusLine: Boolean
	# Method of ordering records; lowest number comes first
	SeqNo: Int
}
