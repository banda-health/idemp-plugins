# Generated Schema for C_Charge - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 13 - $Id$
extend type Query {
	C_Charge(UU: String!): C_Charge
	C_ChargeGet(Page: Int, Size: Int, Sort: String, Filter: String): C_ChargeConnection!
}

extend type Mutation {
	C_ChargeSave(Entity: C_ChargeInput!): C_Charge!
	C_ChargeSaveMany(Entities: [C_ChargeInput!]!): [C_Charge!]!
	C_ChargeDelete(UUs: [String!]!): Boolean!
}

type C_ChargeConnection {
	Results: [C_Charge!]!
	PagingInfo: PagingInfo!
}

type C_Charge {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Determines whether a record is locked or not (must configure each field to enabled/disabled to read from this field)
	BH_Locked: Boolean!
	# Meant to be a sub-type of the charge type
	BH_SubType: AD_Ref_List
	# Identifies a Business Partner
	C_BPartner: C_BPartner
	UU: ID!
	C_ChargeType: C_ChargeType
	# Tax Category
	C_TaxCategory: C_TaxCategory!
	# Charge Amount
	ChargeAmt: BigDecimal!
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean!
	IsSameCurrency: Boolean!
	# Use the same tax as the main transaction
	IsSameTax: Boolean!
	# Tax is included in the price 
	IsTaxIncluded: Boolean!
	# Alphanumeric identifier of the entity
	Name: String!
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
}

input C_ChargeInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Determines whether a record is locked or not (must configure each field to enabled/disabled to read from this field)
	BH_Locked: Boolean
	# Meant to be a sub-type of the charge type
	BH_SubType: ForeignEntityInput
	# Identifies a Business Partner
	C_BPartner: ForeignEntityInput
	UU: ID
	C_ChargeType: ForeignEntityInput
	# Tax Category
	C_TaxCategory: ForeignEntityInput
	# Charge Amount
	ChargeAmt: BigDecimal
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean
	IsSameCurrency: Boolean
	# Use the same tax as the main transaction
	IsSameTax: Boolean
	# Tax is included in the price 
	IsTaxIncluded: Boolean
	# Alphanumeric identifier of the entity
	Name: String
}
