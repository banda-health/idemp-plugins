# Generated Schema for C_ElementValue - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 11 - $Id$
extend type Query {
	C_ElementValueGet(Page: Int, Size: Int, Sort: String, Filter: String): C_ElementValueConnection!
}

extend type Mutation {
	C_ElementValueSave(Entity: C_ElementValueInput!): C_ElementValue!
	C_ElementValueSaveMany(Entities: [C_ElementValueInput!]!): [C_ElementValue!]!
	C_ElementValueDelete(UUs: [String!]!): Boolean!
}

type C_ElementValueConnection {
	Results: [C_ElementValue!]!
	PagingInfo: PagingInfo!
}

type C_ElementValue {
	# Indicates the Natural Sign of the Account as a Debit or Credit
	AccountSign: AD_Ref_List!
	# Indicates the type of account
	AccountType: AD_Ref_List!
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	BPartnerType: AD_Ref_List
	# Account at the Bank
	C_BankAccount: C_BankAccount
	# The Currency for this record
	C_Currency: C_Currency
	# Accounting Element
	C_Element: C_Element!
	UU: ID!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean!
	# Indicates if this is the Bank Account
	IsBankAccount: Boolean!
	IsDetailBPartner: Boolean!
	IsDetailProduct: Boolean!
	# Control account - If an account is controlled by a document, you cannot post manually to it
	IsDocControlled: Boolean!
	# Balances in foreign currency accounts are held in the nominated currency
	IsForeignCurrency: Boolean!
	# This is a summary entity
	IsSummary: Boolean!
	# Alphanumeric identifier of the entity
	Name: String!
	# Actual Values can be posted
	PostActual: Boolean!
	# Budget values can be posted
	PostBudget: Boolean!
	# Post commitments to this account
	PostEncumbrance: Boolean!
	# Post statistical quantities to this account?
	PostStatistical: Boolean!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
	# Valid from including this date (first day)
	ValidFrom: Date
	# Valid to including this date (last day)
	ValidTo: Date
	# Search key for the record in the format required - must be unique
	Value: String!
}

input C_ElementValueInput {
	# Indicates the Natural Sign of the Account as a Debit or Credit
	AccountSign: ForeignEntityInput
	# Indicates the type of account
	AccountType: ForeignEntityInput
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	BPartnerType: ForeignEntityInput
	# Account at the Bank
	C_BankAccount: ForeignEntityInput
	# The Currency for this record
	C_Currency: ForeignEntityInput
	# Accounting Element
	C_Element: ForeignEntityInput
	UU: ID
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean
	# Indicates if this is the Bank Account
	IsBankAccount: Boolean
	IsDetailBPartner: Boolean
	IsDetailProduct: Boolean
	# Control account - If an account is controlled by a document, you cannot post manually to it
	IsDocControlled: Boolean
	# Balances in foreign currency accounts are held in the nominated currency
	IsForeignCurrency: Boolean
	# This is a summary entity
	IsSummary: Boolean
	# Alphanumeric identifier of the entity
	Name: String
	# Actual Values can be posted
	PostActual: Boolean
	# Budget values can be posted
	PostBudget: Boolean
	# Post commitments to this account
	PostEncumbrance: Boolean
	# Post statistical quantities to this account?
	PostStatistical: Boolean
	# Valid from including this date (first day)
	ValidFrom: Date
	# Valid to including this date (last day)
	ValidTo: Date
	# Search key for the record in the format required - must be unique
	Value: String
}
