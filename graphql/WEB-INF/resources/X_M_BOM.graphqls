# Generated Schema for M_BOM - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 12 - $Id$
extend type Query {
	M_BOM(UU: String!): M_BOM
	M_BOMGet(Page: Int, Size: Int, Sort: String, Filter: String, Where: String, Parameters: [Object!]): M_BOMConnection!
}

extend type Mutation {
	M_BOMSave(Entity: M_BOMInput!): M_BOM!
	M_BOMSaveMany(Entities: [M_BOMInput!]!): [M_BOM!]!
	M_BOMDelete(UUs: [String!]!): Boolean!
}

type M_BOMConnection {
	Results: [M_BOM!]!
	PagingInfo: PagingInfo!
}

type M_BOM {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Type of BOM
	BOMType: AD_Ref_List!
	# The use of the Bill of Material
	BOMUse: AD_Ref_List!
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	# Comment or Hint
	Help: String
	# The record is active in the system
	IsActive: Boolean!
	UU: ID!
	# Bill of Materials (Engineering) Change Notice (Version)
	M_ChangeNotice: M_ChangeNotice
	# Product, Service, Item
	M_Product: M_Product!
	# Alphanumeric identifier of the entity
	Name: String!
	Processing: Boolean!
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
}

input M_BOMInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Type of BOM
	BOMType: ForeignEntityInput
	# The use of the Bill of Material
	BOMUse: ForeignEntityInput
	# Optional short description of the record
	Description: String
	# Comment or Hint
	Help: String
	# The record is active in the system
	IsActive: Boolean
	UU: ID
	# Bill of Materials (Engineering) Change Notice (Version)
	M_ChangeNotice: ForeignEntityInput
	# Product, Service, Item
	M_Product: ForeignEntityInput
	# Alphanumeric identifier of the entity
	Name: String
	Processing: Boolean
}
