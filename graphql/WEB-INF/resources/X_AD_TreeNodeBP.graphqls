# Generated Schema for AD_TreeNodeBP - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	AD_TreeNodeBPGet(page: Int, size: Int, sort: String, filter: String): AD_TreeNodeBPConnection!
}

extend type Mutation {
	AD_TreeNodeBPSave(entity: AD_TreeNodeBPInput!): AD_TreeNodeBP!
	AD_TreeNodeBPDelete(uuids: [String!]!): Boolean!
}

type AD_TreeNodeBPConnection {
	results: [AD_TreeNodeBP!]!
	pagingInfo: PagingInfo!
}

type AD_TreeNodeBP {
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# Identifies a Tree
	AD_Tree: AD_Tree!
	ID: ID!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# The record is active in the system
	IsActive: Boolean!
	# Method of ordering records; lowest number comes first
	SeqNo: Int
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
}

input AD_TreeNodeBPInput {
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# Identifies a Tree
	AD_Tree: ForeignEntityInput
	ID: ID
	# The record is active in the system
	IsActive: Boolean
	# Method of ordering records; lowest number comes first
	SeqNo: Int
}
