# Generated Schema for T_BOMLine - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 13 - $Id$
extend type Query {
	T_BOMLine(UU: String!): T_BOMLine
	T_BOMLineGet(Page: Int, Size: Int, Sort: String, Filter: String): T_BOMLineConnection!
}

extend type Mutation {
	T_BOMLineSave(Entity: T_BOMLineInput!): T_BOMLine!
	T_BOMLineSaveMany(Entities: [T_BOMLineInput!]!): [T_BOMLine!]!
	T_BOMLineDelete(UUs: [String!]!): Boolean!
}

type T_BOMLineConnection {
	Results: [T_BOMLine!]!
	PagingInfo: PagingInfo!
}

type T_BOMLine {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Instance of the process
	AD_PInstance: AD_PInstance
	# Rules for accounting
	C_AcctSchema: C_AcctSchema
	# Cost information
	Cost: BigDecimal
	# Indicates how Costs will be calculated
	CostingMethod: AD_Ref_List
	# Standard Costs
	CostStandard: BigDecimal
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# The currently used cost price
	CurrentCostPrice: BigDecimal
	# Current Price Lower Level Is the sum of the costs of the components of this product manufactured for this level.
	CurrentCostPriceLL: BigDecimal
	FutureCostPrice: BigDecimal
	FutureCostPriceLL: BigDecimal
	# Implosion of a Bill of Materials refers to finding all the BOM''s in which a component is used.
	Implosion: Boolean!
	# The record is active in the system
	IsActive: Boolean!
	# Indicated that the Standard Cost is frozen
	IsCostFrozen: Boolean!
	LevelNo: Int
	Levels: String
	# Product Cost Element
	M_CostElement: M_CostElement
	# Type of Cost (e.g. Current, Plan, Future)
	M_CostType: M_CostType
	# Product, Service, Item
	M_Product: M_Product
	# BOM & Formula
	PP_Product_BOM: PP_Product_BOM
	# BOM Line
	PP_Product_BOMLine: PP_Product_BOMLine
	# Indicate the Quantity use in this BOM
	QtyBOM: BigDecimal
	# Method of ordering records; lowest number comes first
	SeqNo: Int
	UU: ID!
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
}

input T_BOMLineInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Instance of the process
	AD_PInstance: ForeignEntityInput
	# Rules for accounting
	C_AcctSchema: ForeignEntityInput
	# Cost information
	Cost: BigDecimal
	# Indicates how Costs will be calculated
	CostingMethod: ForeignEntityInput
	# Standard Costs
	CostStandard: BigDecimal
	# The currently used cost price
	CurrentCostPrice: BigDecimal
	# Current Price Lower Level Is the sum of the costs of the components of this product manufactured for this level.
	CurrentCostPriceLL: BigDecimal
	FutureCostPrice: BigDecimal
	FutureCostPriceLL: BigDecimal
	# Implosion of a Bill of Materials refers to finding all the BOM''s in which a component is used.
	Implosion: Boolean
	# The record is active in the system
	IsActive: Boolean
	# Indicated that the Standard Cost is frozen
	IsCostFrozen: Boolean
	LevelNo: Int
	Levels: String
	# Product Cost Element
	M_CostElement: ForeignEntityInput
	# Type of Cost (e.g. Current, Plan, Future)
	M_CostType: ForeignEntityInput
	# Product, Service, Item
	M_Product: ForeignEntityInput
	# BOM & Formula
	PP_Product_BOM: ForeignEntityInput
	# BOM Line
	PP_Product_BOMLine: ForeignEntityInput
	# Indicate the Quantity use in this BOM
	QtyBOM: BigDecimal
	# Method of ordering records; lowest number comes first
	SeqNo: Int
	UU: ID
}
