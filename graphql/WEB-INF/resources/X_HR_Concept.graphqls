# Generated Schema for HR_Concept - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 11 - $Id$
extend type Query {
	HR_Concept(UU: String!): HR_Concept
	HR_ConceptGet(Page: Int, Size: Int, Sort: String, Filter: String): HR_ConceptConnection!
}

extend type Mutation {
	HR_ConceptSave(Entity: HR_ConceptInput!): HR_Concept!
	HR_ConceptSaveMany(Entities: [HR_ConceptInput!]!): [HR_Concept!]!
	HR_ConceptDelete(UUs: [String!]!): Boolean!
}

type HR_ConceptConnection {
	Results: [HR_Concept!]!
	PagingInfo: PagingInfo!
}

type HR_Concept {
	# Indicates the Natural Sign of the Account as a Debit or Credit
	AccountSign: AD_Ref_List
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# System Reference and Validation
	AD_Reference: AD_Reference
	ColumnType: AD_Ref_List!
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	HR_Concept_Category: HR_Concept_Category
	UU: ID!
	HR_Department: HR_Department
	HR_Job: HR_Job
	HR_Payroll: HR_Payroll
	# The record is active in the system
	IsActive: Boolean!
	# Default value
	IsDefault: Boolean!
	# Indicates if  this Business Partner is an employee
	IsEmployee: Boolean!
	# The document is fully paid
	IsPaid: Boolean!
	# Indicates if this document / line is printed
	IsPrinted: Boolean!
	# Field is read / write
	IsReadWrite: Boolean!
	# This is a sales transaction (receipt)
	IsReceipt: Boolean!
	# The application is registered.
	IsRegistered: Boolean!
	# Alphanumeric identifier of the entity
	Name: String!
	# Method of ordering records; lowest number comes first
	SeqNo: Int
	# Type of Validation (SQL, Java Script, Java Language)
	Type: AD_Ref_List!
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
	# Valid from including this date (first day)
	ValidFrom: DateTime
	# Valid to including this date (last day)
	ValidTo: DateTime
	# Search key for the record in the format required - must be unique
	Value: String
}

input HR_ConceptInput {
	# Indicates the Natural Sign of the Account as a Debit or Credit
	AccountSign: ForeignEntityInput
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# System Reference and Validation
	AD_Reference: ForeignEntityInput
	ColumnType: ForeignEntityInput
	# Optional short description of the record
	Description: String
	HR_Concept_Category: ForeignEntityInput
	UU: ID
	HR_Department: ForeignEntityInput
	HR_Job: ForeignEntityInput
	HR_Payroll: ForeignEntityInput
	# The record is active in the system
	IsActive: Boolean
	# Default value
	IsDefault: Boolean
	# Indicates if  this Business Partner is an employee
	IsEmployee: Boolean
	# The document is fully paid
	IsPaid: Boolean
	# Indicates if this document / line is printed
	IsPrinted: Boolean
	# Field is read / write
	IsReadWrite: Boolean
	# This is a sales transaction (receipt)
	IsReceipt: Boolean
	# The application is registered.
	IsRegistered: Boolean
	# Alphanumeric identifier of the entity
	Name: String
	# Method of ordering records; lowest number comes first
	SeqNo: Int
	# Type of Validation (SQL, Java Script, Java Language)
	Type: ForeignEntityInput
	# Valid from including this date (first day)
	ValidFrom: DateTime
	# Valid to including this date (last day)
	ValidTo: DateTime
	# Search key for the record in the format required - must be unique
	Value: String
}
