# Generated Schema for AD_Registration - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 13 - $Id$
extend type Query {
	AD_Registration(UU: String!): AD_Registration
	AD_RegistrationGet(Page: Int, Size: Int, Sort: String, Filter: String): AD_RegistrationConnection!
}

extend type Mutation {
	AD_RegistrationSave(Entity: AD_RegistrationInput!): AD_Registration!
	AD_RegistrationSaveMany(Entities: [AD_RegistrationInput!]!): [AD_Registration!]!
	AD_RegistrationDelete(UUs: [String!]!): Boolean!
}

type AD_RegistrationConnection {
	Results: [AD_Registration!]!
	PagingInfo: PagingInfo!
}

type AD_Registration {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	UU: ID!
	# System Definition
	AD_System: AD_System!
	# The Currency for this record
	C_Currency: C_Currency
	# Location or Address
	C_Location: C_Location
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	# Information of the industry (e.g. professional service, distribution of furnitures, ..)
	IndustryInfo: String
	# The record is active in the system
	IsActive: Boolean!
	# You allow to publish the information, not just statistical summary info
	IsAllowPublish: Boolean!
	# Maintain general statistics
	IsAllowStatistics: Boolean!
	# The system is in production
	IsInProduction: Boolean!
	# The application is registered.
	IsRegistered: Boolean!
	# Number of employees
	NumberEmployees: Int
	# Information about Server and Tenant Platform
	PlatformInfo: String
	Processing: Boolean!
	# Direct internal record ID
	Record_ID: Int
	# Remote Address
	Remote_Addr: String
	# Remote host Info
	Remote_Host: String
	# Total Volume of Sales in Thousands of Currency
	SalesVolume: Int
	# The day you started the implementation (if implementing) - or production (went life) with iDempiere
	StartProductionDate: Date
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
}

input AD_RegistrationInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	UU: ID
	# System Definition
	AD_System: ForeignEntityInput
	# The Currency for this record
	C_Currency: ForeignEntityInput
	# Location or Address
	C_Location: ForeignEntityInput
	# Optional short description of the record
	Description: String
	# Information of the industry (e.g. professional service, distribution of furnitures, ..)
	IndustryInfo: String
	# The record is active in the system
	IsActive: Boolean
	# You allow to publish the information, not just statistical summary info
	IsAllowPublish: Boolean
	# Maintain general statistics
	IsAllowStatistics: Boolean
	# The system is in production
	IsInProduction: Boolean
	# The application is registered.
	IsRegistered: Boolean
	# Number of employees
	NumberEmployees: Int
	# Information about Server and Tenant Platform
	PlatformInfo: String
	Processing: Boolean
	# Direct internal record ID
	Record_ID: Int
	# Remote Address
	Remote_Addr: String
	# Remote host Info
	Remote_Host: String
	# Total Volume of Sales in Thousands of Currency
	SalesVolume: Int
	# The day you started the implementation (if implementing) - or production (went life) with iDempiere
	StartProductionDate: Date
}
