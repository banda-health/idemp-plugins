# Generated Schema for R_CategoryUpdates - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	R_CategoryUpdatesGet(page: Int, size: Int, sort: String, filter: String): R_CategoryUpdatesConnection!
}

extend type Mutation {
	R_CategoryUpdatesSave(entity: R_CategoryUpdatesInput!): R_CategoryUpdates!
	R_CategoryUpdatesSaveMany(entities: [R_CategoryUpdatesInput!]!): [R_CategoryUpdates!]!
	R_CategoryUpdatesDelete(uuids: [String!]!): Boolean!
}

type R_CategoryUpdatesConnection {
	results: [R_CategoryUpdates!]!
	pagingInfo: PagingInfo!
}

type R_CategoryUpdates {
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# User within the system - Internal or Business Partner Contact
	AD_User: AD_User!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# The record is active in the system
	IsActive: Boolean!
	# This is a Self-Service entry or this entry can be changed via Self-Service
	IsSelfService: Boolean!
	# Request Category
	R_Category: R_Category!
	UUID: ID!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
}

input R_CategoryUpdatesInput {
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# User within the system - Internal or Business Partner Contact
	AD_User: ForeignEntityInput
	# The record is active in the system
	IsActive: Boolean
	# This is a Self-Service entry or this entry can be changed via Self-Service
	IsSelfService: Boolean
	# Request Category
	R_Category: ForeignEntityInput
	UUID: ID
}
