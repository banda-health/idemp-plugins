# Generated Schema for AD_Tree - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	AD_TreeGet(page: Int, size: Int, sort: String, filter: String): AD_TreeConnection!
}

extend type Mutation {
	AD_TreeSave(entity: AD_TreeInput!): AD_Tree!
	AD_TreeDelete(uuids: [String!]!): Boolean!
}

type AD_TreeConnection {
	results: [AD_Tree!]!
	pagingInfo: PagingInfo!
}

type AD_Tree {
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# Database Table information
	AD_Table: AD_Table
	ID: ID!
	# Date this record was created
	Created: Date!
	# User who created this records
	Create: AD_User!
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean!
	# All Nodes are included (Complete Tree)
	IsAllNodes: Boolean!
	# Default value
	IsDefault: Boolean!
	# If checked, all nodes are loaded before tree is displayed
	IsLoadAllNodesImmediately: Boolean!
	IsTreeDrivenByValue: Boolean!
	# Displays Value column with the Display column
	IsValueDisplayed: Boolean!
	# Alphanumeric identifier of the entity
	Name: String!
	# The link column on the parent tab.
	Parent_Column: AD_Column
	Processing: Boolean!
	# Element this tree is built on (i.e Product, Business Partner)
	TreeType_RL: AD_Ref_List!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	Update: AD_User!
}

input AD_TreeInput {
	# Organizational entity within client
	AD_Org: AD_OrgInput
	# Database Table information
	AD_Table: AD_TableInput
	ID: ID
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean
	# All Nodes are included (Complete Tree)
	IsAllNodes: Boolean
	# Default value
	IsDefault: Boolean
	# If checked, all nodes are loaded before tree is displayed
	IsLoadAllNodesImmediately: Boolean
	IsTreeDrivenByValue: Boolean
	# Displays Value column with the Display column
	IsValueDisplayed: Boolean
	# Alphanumeric identifier of the entity
	Name: String
	# The link column on the parent tab.
	Parent_Column: AD_ColumnInput
	Processing: Boolean
	# Element this tree is built on (i.e Product, Business Partner)
	TreeType_RL: AD_Ref_ListInput
}
