# Generated Schema for AD_TreeNodeCMM - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 12 - $Id$
extend type Query {
	AD_TreeNodeCMM(UU: String!): AD_TreeNodeCMM
	AD_TreeNodeCMMGet(Page: Int, Size: Int, Sort: String, Filter: String): AD_TreeNodeCMMConnection!
}

extend type Mutation {
	AD_TreeNodeCMMSave(Entity: AD_TreeNodeCMMInput!): AD_TreeNodeCMM!
	AD_TreeNodeCMMSaveMany(Entities: [AD_TreeNodeCMMInput!]!): [AD_TreeNodeCMM!]!
	AD_TreeNodeCMMDelete(UUs: [String!]!): Boolean!
}

type AD_TreeNodeCMMConnection {
	Results: [AD_TreeNodeCMM!]!
	PagingInfo: PagingInfo!
}

type AD_TreeNodeCMM {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# Identifies a Tree
	AD_Tree: AD_Tree!
	UU: ID!
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# The record is active in the system
	IsActive: Boolean!
	# Method of ordering records; lowest number comes first
	SeqNo: Int!
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
}

input AD_TreeNodeCMMInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# Identifies a Tree
	AD_Tree: ForeignEntityInput
	UU: ID
	# The record is active in the system
	IsActive: Boolean
	# Method of ordering records; lowest number comes first
	SeqNo: Int
}
