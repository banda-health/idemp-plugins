# Generated Schema for A_Asset_Group - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	A_Asset_GroupGet(page: Int, size: Int, sort: String, filter: String): A_Asset_GroupConnection!
}

extend type Mutation {
	A_Asset_GroupSave(entity: A_Asset_GroupInput!): A_Asset_Group!
	A_Asset_GroupSaveMany(entities: [A_Asset_GroupInput!]!): [A_Asset_Group!]!
	A_Asset_GroupDelete(uuids: [String!]!): Boolean!
}

type A_Asset_GroupConnection {
	results: [A_Asset_Group!]!
	pagingInfo: PagingInfo!
}

type A_Asset_Group {
	A_Asset_Class: A_Asset_Class
	UUID: ID!
	A_Asset_Type: A_Asset_Type
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	# Comment or Hint
	Help: String
	# The record is active in the system
	IsActive: Boolean!
	# Create Asset and activate it
	IsCreateAsActive: Boolean!
	# Default value
	IsDefault: Boolean!
	# The asset will be depreciated
	IsDepreciated: Boolean!
	IsFixedAsset: Boolean!
	# Create one asset per UOM
	IsOneAssetPerUOM: Boolean!
	# The asset is owned by the organization
	IsOwned: Boolean!
	# Enable tracking issues for this asset
	IsTrackIssues: Boolean!
	# Alphanumeric identifier of the entity
	Name: String!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
}

input A_Asset_GroupInput {
	A_Asset_Class: ForeignEntityInput
	UUID: ID
	A_Asset_Type: ForeignEntityInput
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# Optional short description of the record
	Description: String
	# Comment or Hint
	Help: String
	# The record is active in the system
	IsActive: Boolean
	# Create Asset and activate it
	IsCreateAsActive: Boolean
	# Default value
	IsDefault: Boolean
	# The asset will be depreciated
	IsDepreciated: Boolean
	IsFixedAsset: Boolean
	# Create one asset per UOM
	IsOneAssetPerUOM: Boolean
	# The asset is owned by the organization
	IsOwned: Boolean
	# Enable tracking issues for this asset
	IsTrackIssues: Boolean
	# Alphanumeric identifier of the entity
	Name: String
}
