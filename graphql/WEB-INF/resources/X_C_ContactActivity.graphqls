# Generated Schema for C_ContactActivity - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	C_ContactActivityGet(page: Int, size: Int, sort: String, filter: String): C_ContactActivityConnection!
}

extend type Mutation {
	C_ContactActivitySave(entity: C_ContactActivityInput!): C_ContactActivity!
	C_ContactActivityDelete(uuids: [String!]!): Boolean!
}

type C_ContactActivityConnection {
	results: [C_ContactActivity!]!
	pagingInfo: PagingInfo!
}

type C_ContactActivity {
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# User within the system - Internal or Business Partner Contact
	AD_User: AD_User
	ID: ID!
	C_Opportunity: C_Opportunity
	# Comments or additional information
	Comments: String
	# Type of activity, e.g. task, email, phone call
	ContactActivityType: AD_Ref_List!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String!
	# Last effective date (inclusive)
	EndDate: Date
	# The record is active in the system
	IsActive: Boolean!
	# It is complete
	IsComplete: Boolean!
	# Sales Representative or Company Agent
	SalesRep: AD_User
	# First effective day (inclusive)
	StartDate: Date!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
}

input C_ContactActivityInput {
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# User within the system - Internal or Business Partner Contact
	AD_User: ForeignEntityInput
	ID: ID
	C_Opportunity: ForeignEntityInput
	# Comments or additional information
	Comments: String
	# Type of activity, e.g. task, email, phone call
	ContactActivityType: AD_Ref_ListInput
	# Optional short description of the record
	Description: String
	# Last effective date (inclusive)
	EndDate: Date
	# The record is active in the system
	IsActive: Boolean
	# It is complete
	IsComplete: Boolean
	# Sales Representative or Company Agent
	SalesRep: ForeignEntityInput
	# First effective day (inclusive)
	StartDate: Date
}
