# Generated Schema for M_Product_BOM - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	M_Product_BOMGet(page: Int, size: Int, sort: String, filter: String): M_Product_BOMConnection!
}

extend type Mutation {
	M_Product_BOMSave(entity: M_Product_BOMInput!): M_Product_BOM!
	M_Product_BOMSaveMany(entities: [M_Product_BOMInput!]!): [M_Product_BOM!]!
	M_Product_BOMDelete(uuids: [String!]!): Boolean!
}

type M_Product_BOMConnection {
	results: [M_Product_BOM!]!
	pagingInfo: PagingInfo!
}

type M_Product_BOM {
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# Bill of Materials Quantity
	BOMQty: BigDecimal!
	# Type of BOM
	BOMType: AD_Ref_List
	# Standard Costs
	CostStandard: BigDecimal
	# Standard Cost Invoice Amount Sum (internal)
	CostStandardCumAmt: BigDecimal
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean!
	# Bill of Materials
	IsBillOfMaterial: Boolean!
	# Unique line for this document
	Line: Int!
	M_PartType: M_PartType
	UUID: ID!
	# Product, Service, Item
	M_Product: M_Product!
	# Bill of Material Component Product
	M_ProductBOM: M_Product!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
	# Search key for the record in the format required - must be unique
	Value: String
}

input M_Product_BOMInput {
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# Bill of Materials Quantity
	BOMQty: BigDecimal
	# Type of BOM
	BOMType: AD_Ref_ListInput
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean
	# Unique line for this document
	Line: Int
	UUID: ID
	# Product, Service, Item
	M_Product: ForeignEntityInput
	# Bill of Material Component Product
	M_ProductBOM: ForeignEntityInput
}
