# Generated Schema for M_PackageMPS - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	M_PackageMPSGet(page: Int, size: Int, sort: String, filter: String): M_PackageMPSConnection!
}

extend type Mutation {
	M_PackageMPSSave(entity: M_PackageMPSInput!): M_PackageMPS!
	M_PackageMPSDelete(uuids: [String!]!): Boolean!
}

type M_PackageMPSConnection {
	results: [M_PackageMPS!]!
	pagingInfo: PagingInfo!
}

type M_PackageMPS {
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# Standard Unit of Measure for Length
	C_UOM_Length: C_UOM
	# Standard Unit of Measure for Weight
	C_UOM_Weight: C_UOM
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# Process which will generate a new document lines based on an existing document
	CreateFrom: String
	# Optional short description of the record
	Description: String
	EstimatedWeight: Int
	Height: Int
	# The record is active in the system
	IsActive: Boolean!
	Length: Int
	# Shipment Package
	M_Package: M_Package!
	ID: ID!
	MasterTrackingNo: String
	# Price
	Price: Int
	# The document has been processed
	Processed: Boolean!
	# Method of ordering records; lowest number comes first
	SeqNo: Int
	# Number to track the shipment
	TrackingNo: String
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
	# Weight of a product
	Weight: Int
	Width: Int
}

input M_PackageMPSInput {
	# Organizational entity within client
	AD_Org: AD_OrgInput
	# Standard Unit of Measure for Length
	C_UOM_Length: C_UOMInput
	# Standard Unit of Measure for Weight
	C_UOM_Weight: C_UOMInput
	# Process which will generate a new document lines based on an existing document
	CreateFrom: String
	# Optional short description of the record
	Description: String
	Height: Int
	# The record is active in the system
	IsActive: Boolean
	Length: Int
	# Shipment Package
	M_Package: M_PackageInput
	ID: ID
	MasterTrackingNo: String
	# Price
	Price: Int
	# The document has been processed
	Processed: Boolean
	# Method of ordering records; lowest number comes first
	SeqNo: Int
	# Number to track the shipment
	TrackingNo: String
	# Weight of a product
	Weight: Int
	Width: Int
}
