# Generated Schema for M_BOMProduct - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 13 - $Id$
extend type Query {
	M_BOMProduct(UU: String!): M_BOMProduct
	M_BOMProductGet(Page: Int, Size: Int, Sort: String, Filter: String): M_BOMProductConnection!
}

extend type Mutation {
	M_BOMProductSave(Entity: M_BOMProductInput!): M_BOMProduct!
	M_BOMProductSaveMany(Entities: [M_BOMProductInput!]!): [M_BOMProduct!]!
	M_BOMProductDelete(UUs: [String!]!): Boolean!
}

type M_BOMProductConnection {
	Results: [M_BOMProduct!]!
	PagingInfo: PagingInfo!
}

type M_BOMProduct {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# BOM Product Type
	BOMProductType: AD_Ref_List!
	# Bill of Materials Quantity
	BOMQty: BigDecimal!
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	# Comment or Hint
	Help: String
	# The record is active in the system
	IsActive: Boolean!
	# Phantom Component
	IsPhantom: Boolean!
	# Optional Lead Time offset before starting production
	LeadTimeOffset: Int!
	# Unique line for this document
	Line: Int!
	# Product Attribute Set Instance
	M_AttributeSetInstance: M_AttributeSetInstance
	# Bill of Material
	M_BOM: M_BOM!
	# Product BOM Alternative Group
	M_BOMAlternative: M_BOMAlternative
	UU: ID!
	# Bill of Materials (Engineering) Change Notice (Version)
	M_ChangeNotice: M_ChangeNotice
	# Bill of Material Component Product
	M_ProductBOM: M_Product
	# Product Manufacturing Operation
	M_ProductOperation: M_ProductOperation
	# Method of ordering records; lowest number comes first
	SeqNo: Int
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
}

input M_BOMProductInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# BOM Product Type
	BOMProductType: ForeignEntityInput
	# Bill of Materials Quantity
	BOMQty: BigDecimal
	# Optional short description of the record
	Description: String
	# Comment or Hint
	Help: String
	# The record is active in the system
	IsActive: Boolean
	# Phantom Component
	IsPhantom: Boolean
	# Optional Lead Time offset before starting production
	LeadTimeOffset: Int
	# Unique line for this document
	Line: Int
	# Product Attribute Set Instance
	M_AttributeSetInstance: ForeignEntityInput
	# Bill of Material
	M_BOM: ForeignEntityInput
	# Product BOM Alternative Group
	M_BOMAlternative: ForeignEntityInput
	UU: ID
	# Bill of Materials (Engineering) Change Notice (Version)
	M_ChangeNotice: ForeignEntityInput
	# Bill of Material Component Product
	M_ProductBOM: ForeignEntityInput
	# Product Manufacturing Operation
	M_ProductOperation: ForeignEntityInput
	# Method of ordering records; lowest number comes first
	SeqNo: Int
}
