# Generated Schema for AD_User_OrgAccess - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 7.1 - $Id$
extend type Query {
	AD_User_OrgAccessGet(page: Int, size: Int, sort: String, filter: String): AD_User_OrgAccessConnection!
}

extend type Mutation {
	AD_User_OrgAccessSave(entity: AD_User_OrgAccessInput!): AD_User_OrgAccess!
	AD_User_OrgAccessDelete(uuids: [String!]!): Boolean!
}

type AD_User_OrgAccessConnection {
	results: [AD_User_OrgAccess!]!
	pagingInfo: PagingInfo!
}

type AD_User_OrgAccess {
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# User within the system - Internal or Business Partner Contact
	AD_User: AD_User!
	ID: ID!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# The record is active in the system
	IsActive: Boolean!
	# Field is read only
	IsReadOnly: Boolean!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
}

input AD_User_OrgAccessInput {
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# User within the system - Internal or Business Partner Contact
	AD_User: ForeignEntityInput
	ID: ID
	# The record is active in the system
	IsActive: Boolean
	# Field is read only
	IsReadOnly: Boolean
}
