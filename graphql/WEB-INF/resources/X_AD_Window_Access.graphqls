# Generated Schema for AD_Window_Access - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	AD_Window_AccessGet(page: Int, size: Int, sort: String, filter: String): AD_Window_AccessConnection!
}

extend type Mutation {
	AD_Window_AccessSave(entity: AD_Window_AccessInput!): AD_Window_Access!
	AD_Window_AccessDelete(uuids: [String!]!): Boolean!
}

type AD_Window_AccessConnection {
	results: [AD_Window_Access!]!
	pagingInfo: PagingInfo!
}

type AD_Window_Access {
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# Responsibility Role
	AD_Role: AD_Role!
	ID: ID!
	# Data entry or display window
	AD_Window: AD_Window!
	BH_CanDeactivate: Boolean!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# The record is active in the system
	IsActive: Boolean!
	# Field is read / write
	IsReadWrite: Boolean!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
}

input AD_Window_AccessInput {
	# Organizational entity within client
	AD_Org: ForeignEntityInput
	# Responsibility Role
	AD_Role: ForeignEntityInput
	ID: ID
	# Data entry or display window
	AD_Window: ForeignEntityInput
	BH_CanDeactivate: Boolean
	# The record is active in the system
	IsActive: Boolean
	# Field is read / write
	IsReadWrite: Boolean
}
