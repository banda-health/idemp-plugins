# Generated Schema for C_OrgAssignment - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 11 - $Id$
extend type Query {
	C_OrgAssignment(UU: String!): C_OrgAssignment
	C_OrgAssignmentGet(Page: Int, Size: Int, Sort: String, Filter: String): C_OrgAssignmentConnection!
}

extend type Mutation {
	C_OrgAssignmentSave(Entity: C_OrgAssignmentInput!): C_OrgAssignment!
	C_OrgAssignmentSaveMany(Entities: [C_OrgAssignmentInput!]!): [C_OrgAssignment!]!
	C_OrgAssignmentDelete(UUs: [String!]!): Boolean!
}

type C_OrgAssignmentConnection {
	Results: [C_OrgAssignment!]!
	PagingInfo: PagingInfo!
}

type C_OrgAssignment {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	# User within the system - Internal or Business Partner Contact
	AD_User: AD_User!
	UU: ID!
	# Date this record was created
	Created: DateTime!
	# User who created this records
	CreatedBy: AD_User!
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean!
	# Date this record was updated
	Updated: DateTime!
	# User who updated this records
	UpdatedBy: AD_User!
	# Valid from including this date (first day)
	ValidFrom: Date!
	# Valid to including this date (last day)
	ValidTo: Date
}

input C_OrgAssignmentInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	# User within the system - Internal or Business Partner Contact
	AD_User: ForeignEntityInput
	UU: ID
	# Optional short description of the record
	Description: String
	# The record is active in the system
	IsActive: Boolean
	# Valid from including this date (first day)
	ValidFrom: Date
	# Valid to including this date (last day)
	ValidTo: Date
}
