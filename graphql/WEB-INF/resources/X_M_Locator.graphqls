# Generated Schema for M_Locator - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 8.2 - $Id$
extend type Query {
	M_LocatorGet(page: Int, size: Int, sort: String, filter: String): M_LocatorConnection!
}

extend type Mutation {
	M_LocatorSave(entity: M_LocatorInput!): M_Locator!
	M_LocatorDelete(uuids: [String!]!): Boolean!
}

type M_LocatorConnection {
	results: [M_Locator!]!
	pagingInfo: PagingInfo!
}

type M_Locator {
	# Client/Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within client
	AD_Org: AD_Org!
	# Date this record was created
	Created: Date!
	# User who created this records
	Create: AD_User!
	# The record is active in the system
	IsActive: Boolean!
	# Default value
	IsDefault: Boolean!
	ID: ID!
	M_LocatorType: M_LocatorType
	# Storage Warehouse and Service Point
	M_Warehouse: M_Warehouse!
	# Where inventory should be picked from first
	PriorityNo: Int!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	Update: AD_User!
	# Search key for the record in the format required - must be unique
	Value: String!
	# X dimension, e.g., Aisle
	X: String!
	# Y dimension, e.g., Bin
	Y: String!
	# Z dimension, e.g., Level
	Z: String!
}

input M_LocatorInput {
	# Organizational entity within client
	AD_Org: AD_OrgInput
	# The record is active in the system
	IsActive: Boolean
	# Default value
	IsDefault: Boolean
	ID: ID
	M_LocatorType: M_LocatorTypeInput
	# Storage Warehouse and Service Point
	M_Warehouse: M_WarehouseInput
	# Where inventory should be picked from first
	PriorityNo: Int
	# Search key for the record in the format required - must be unique
	Value: String
	# X dimension, e.g., Aisle
	X: String
	# Y dimension, e.g., Bin
	Y: String
	# Z dimension, e.g., Level
	Z: String
}
