# Generated Schema for R_RequestProcessor - DO NOT CHANGE
#		@author Banda Health (generated) 
#		@version Release 11 - $Id$
extend type Query {
	R_RequestProcessorGet(Page: Int, Size: Int, Sort: String, Filter: String): R_RequestProcessorConnection!
}

extend type Mutation {
	R_RequestProcessorSave(Entity: R_RequestProcessorInput!): R_RequestProcessor!
	R_RequestProcessorSaveMany(Entities: [R_RequestProcessorInput!]!): [R_RequestProcessor!]!
	R_RequestProcessorDelete(UUs: [String!]!): Boolean!
}

type R_RequestProcessorConnection {
	Results: [R_RequestProcessor!]!
	PagingInfo: PagingInfo!
}

type R_RequestProcessor {
	# Tenant for this installation.
	AD_Client: AD_Client!
	# Organizational entity within tenant
	AD_Org: AD_Org!
	AD_Schedule: AD_Schedule!
	# Date this record was created
	Created: Date!
	# User who created this records
	CreatedBy: AD_User!
	# Date the process was last run.
	DateLastRun: Date
	# Date the process will run next
	DateNextRun: Date
	# Optional short description of the record
	Description: String
	# Send Alert when there is no activity after days (0= no alert)
	InactivityAlertDays: Int!
	# The record is active in the system
	IsActive: Boolean!
	# Number of days to keep the log entries
	KeepLogDays: Int!
	# Alphanumeric identifier of the entity
	Name: String!
	# Send email alert after number of days due (0=no alerts)
	OverdueAlertDays: Int!
	# Escalation to superior after number of due days (0 = no)
	OverdueAssignDays: Int!
	Processing: Boolean!
	UU: ID!
	# Type of request (e.g. Inquiry, Complaint, ..)
	R_RequestType: R_RequestType
	# Days between sending Reminder Emails for a due or inactive Document
	RemindDays: Int!
	# Supervisor for this user/organization - used for escalation and approval
	Supervisor: AD_User!
	# Date this record was updated
	Updated: Date!
	# User who updated this records
	UpdatedBy: AD_User!
}

input R_RequestProcessorInput {
	# Organizational entity within tenant
	AD_Org: ForeignEntityInput
	AD_Schedule: ForeignEntityInput
	# Date the process was last run.
	DateLastRun: Date
	# Date the process will run next
	DateNextRun: Date
	# Optional short description of the record
	Description: String
	# Send Alert when there is no activity after days (0= no alert)
	InactivityAlertDays: Int
	# The record is active in the system
	IsActive: Boolean
	# Number of days to keep the log entries
	KeepLogDays: Int
	# Alphanumeric identifier of the entity
	Name: String
	# Send email alert after number of days due (0=no alerts)
	OverdueAlertDays: Int
	# Escalation to superior after number of due days (0 = no)
	OverdueAssignDays: Int
	Processing: Boolean
	UU: ID
	# Type of request (e.g. Inquiry, Complaint, ..)
	R_RequestType: ForeignEntityInput
	# Days between sending Reminder Emails for a due or inactive Document
	RemindDays: Int
	# Supervisor for this user/organization - used for escalation and approval
	Supervisor: ForeignEntityInput
}
